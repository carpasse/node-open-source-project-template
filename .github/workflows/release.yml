name: Release

on:
  push:
    branches: ['master', 'beta', 'alpha']

jobs:
  release:
    uses: carpasse/reusable-workflows/.github/workflows/release.yml@master
    secrets:
      NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
